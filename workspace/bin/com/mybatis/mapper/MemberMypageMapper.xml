<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="memberMypage">
<select id="memberInfo" parameterType="int" resultType="MemberDTO">
      SELECT 
      m.member_id,
      m.member_name,
      m.member_phone,
      m.member_birth,
      ad.address_postal,
      ad.address_address,
      ad.address_detail
      from dt_member m
      left join dt_address ad on m.address_number = ad.address_number
      where m.member_number = #{memberNumber}
</select>

<select id="memberPwOk" parameterType="String" resultType="MemberDTO">
	select count(member_name)
	from dt_member m
	where m.meber_pw = #{memberPw} and m.member_id = #{memberId}
</select>

<select id="memberRecive" parameterType="MemberDTO">
	select count(*)
	from dt_member
	where member_id = #{memberId} and member_pw = #{memberPw}
</select>

<delete id="memberQuit1" parameterType="int">
    DELETE FROM dt_address
    where m.member_number = #{memberNumber}
</delete>

<delete id="memberQuit2" parameterType="int">
    DELETE FROM dt_member
    where m.member_number = #{memberNumber}
</delete>


   
   
 
</mapper>